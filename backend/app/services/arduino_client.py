"""
Arduino / ESP32 stream placeholder.

Replace with real WebSocket or serial client when hardware is connected.
Receives high-fidelity audio (or sensor data) and forwards to STT.
"""

# TODO: Connect to Arduino/ESP32 over WebSocket or serial.
# Example: async def stream_audio() -> AsyncIterator[bytes]: ...
