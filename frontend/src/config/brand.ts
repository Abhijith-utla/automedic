/**
 * Branding: change the app name and logo here.
 *
 * - App name: set APP_NAME below to change the name everywhere (landing, header, login, register).
 * - In-app logo: replace src/assets/logo.png (used on landing, header, login, register).
 * - Browser tab icon: index.html uses /logo.png from public/ — keep public/logo.png in sync or edit index.html.
 */
export const APP_NAME = "Automedic";
